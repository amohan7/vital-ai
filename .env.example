# Copy this file to .env and fill in your values
# .env is gitignored — never commit your actual API key

# Optional: set your Gemini API key here as a server-side fallback.
# The dashboard also lets you enter it directly in the UI (stored in memory only).
GEMINI_API_KEY=your_gemini_api_key_here
